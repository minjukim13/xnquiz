# .env.example — 팀 공유용 키 목록 (실제 값 없음)
# 복사 후 .env.local 로 저장하고 값 입력

# 데이터 소스 토글
# - mock: 브라우저 localStorage + src/data/mockData.js 시드로 동작 (기본값)
# - api : 동일 오리진의 /api/* 백엔드 호출
VITE_DATA_SOURCE=mock

VITE_APP_NAME=XN Quizzes
