# S-02. 퀴즈 생성

> **라벨 규칙**: `[A]` Canvas 기존 기능 / `[B-#NN]` 학교 요구사항 신규 / `[C]` 자체 도출 개선

## 0. 문서 헤더

| **항목** | **내용** |
|---|---|
| 프로젝트 ID | PRJ-XQ-BASE |
| 문서 ID | PRJ-XQ-BASE-SSD-S-02-v3 |
| 작성자 | 김민주 (Creator/PD) |
| 참조 FRD | (TBD) |
| 참조 DS Baseline | LearningX DS Baseline (예정). 임시: SSD [README](README.md) 디바이스 분기점 / 컴포넌트 사용 원칙 절 |

## 1. 화면 정보

| **항목** | **내용** |
|---|---|
| 화면 ID | S-02 |
| 라우트 | `/quiz/new` |
| 진입 권한 | 교수자 (학생 접근 시 자동 리다이렉트) |
| 진입 경로 | 퀴즈 목록(S-01) → "새 퀴즈" 버튼 |
| 우선순위 | P0 |

## 2. 화면 목적

교수자가 신규 퀴즈를 생성하고 기본 설정 + 문항 구성까지 한 화면에서 마무리.

## 3. 레이아웃 (Region Map)

```
[헤더: "새 퀴즈 만들기" 타이틀]
[탭: 기본정보 | 문항구성]
[본문 - 기본정보 탭]
  ├── 퀴즈 유형 (평가용/연습용)
  ├── 기본정보 (제목/설명/주차·차시)
  ├── 응시 기간 (시작/마감/이용종료)
  ├── 추가 기간 (지각 제출 / 학생별 추가 기간)
  ├── 응시 설정 (시간제한/재응시/순서·선택지 셔플/한 문항씩 표시)
  ├── 문항 표시 (한 페이지/한 문항씩)
  ├── 성적 공개 (범위/시점/기간/1회 조회)
  ├── 접근 제한 (액세스 코드 / IP)
  ├── 안내사항 (기본값 자동 채움)
  └── 공개 여부 (학생에게 공개/비공개)
[본문 - 문항구성 탭]
  ├── 문항 추가 버튼 3종 (직접 작성 / 문제은행 / 랜덤 출제)
  └── 문항 리스트 (드래그 정렬)
[푸터: 취소(좌) | 임시저장 + 저장하기(우)]
[모달: 문항 작성 / 문제은행 선택 / 랜덤 출제 / 학생별 추가 기간 / 검증 오류 / 변경 취소 확인]
```

| **영역** | **설명** |
|---|---|
| 헤더 | 타이틀 |
| 탭 | 기본정보 / 문항구성 |
| 본문 (기본정보) | 10개 설정 섹션 |
| 본문 (문항구성) | 문항 추가 3종 + 문항 리스트 (드래그 정렬) |
| 푸터 | 좌: 취소 / 우: 임시저장 + 저장 |
| 모달 | 6종 (문항 작성 / 은행 / 랜덤 / 학생별 추가 / 오류 / 취소 확인) |

## 4. 사용 컴포넌트 (DS Baseline 참조)

| **컴포넌트** | **variant / state** | **용도** |
|---|---|---|
| Tabs | underline | 기본정보 / 문항구성 |
| Input / Textarea | default·error | 제목 / 설명 / 안내사항 |
| RichText | default | 설명 (서식 지원) |
| Select | week-session / shuffle / retake | 주차 / 차시 / 점수 정책 / 셔플 등 |
| DateTimePicker | default | 시작/마감/이용종료/지각 마감 |
| Switch | default·disabled | 시간 제한 / 셔플 / 자동제출 비활성 / 공개여부 |
| Card | section | 10개 설정 섹션 |
| Button | primary / secondary / ghost / destructive | 저장 / 임시저장 / 취소 / 삭제 |
| QuestionList | drag-reorder | 문항 리스트 + 드래그 정렬 |
| Dialog | step-modal (이전 좌 / 취소+다음 우) | 모든 스텝 모달 통일 푸터 |
| AlertDialog | error / confirm | 검증 오류 첫 항목 / 변경 취소 확인 |
| Toast | success / error | 저장 결과 |

## 5. 기능 분류

### 5-1. [A] Canvas 기존 기능 - 기존 Canvas Classic Quizzes 제공 기능 재구현

| **ID** | **기능** | **동작** |
|---|---|---|
| A-02-1 | 제목 / 설명 입력 | 필수값(제목), 설명은 RichText 지원 |
| A-02-2 | 퀴즈 유형 선택 | 평가용 / 연습용 |
| A-02-3 | 응시 기간 설정 | 시작일시 / 마감일시 / 이용 종료일시 |
| A-02-4 | 시간 제한 | 토글 + 분 수 + 자동제출 비활성화 옵션 |
| A-02-5 | 재응시 횟수 + 점수 정책 | 최대 횟수 (2~10/무제한) + 점수 반영 정책 (최고/최신/평균) |
| A-02-6 | 선택지 무작위 / 문항 순서 무작위 | Shuffle 옵션 |
| A-02-7 | 한 문항씩 표시 + 응답 후 잠금 | One question at a time + 잠금 |
| A-02-8 | 학생별 추가 기간 (AssignmentOverrides) | 특정 학생/그룹에게 별도 마감일 부여 |
| A-02-9 | 액세스 코드 / IP 제한 | 응시 진입 시 사전 검증 |
| A-02-10 | 문항 직접 작성 | 12개 유형 모달로 작성 |
| A-02-11 | 문항 드래그 정렬 | 드래그 핸들로 순서 변경 |
| A-02-12 | 안내사항 입력 | 응시 전 학생에게 노출되는 안내 텍스트 |
| A-02-13 | 학생 공개여부 토글 | 저장 시점에 학생에게 노출할지 |
| A-02-14 | 임시저장 / 저장 분리 | 임시저장은 제목만 필수(학생 비공개 상태로 저장), 저장은 전체 검증 |
| A-02-15 | 주차 / 차시 메타 + LTI 자동 프리필 | LMS 과목에 등록된 주차/차시 옵션을 불러와 선택 (과목별 가변). 기본값 1주차 1차시. LearningX 진입 시 localStorage 의 week/session 자동 채움 |

### 5-2. [B] 학교 요구사항 반영 - Canvas 에 없거나 부족하여 학교가 요구한 신규 기능

| **ID** | **라벨** | **기능** | **동작** |
|---|---|---|---|
| F-02-1 | [B-#08] | 단순화된 2탭 구조 (기본정보 / 문항구성) | Canvas 의 다중 탭 구조를 행동 유도형 2탭으로 단순화 |
| F-02-2 | [B-#08] | 한 화면 안에서 설정 + 문항 작성 완결 | 별도 페이지 이동 없이 동일 화면에서 마무리 |
| F-02-3 | [B-#07] | 다중 그룹 랜덤 출제 | 여러 문제은행 그룹에서 각각 N문항씩 랜덤 추출 + 미리보기 |
| F-02-4 | [B-#11] | 조건부 재응시 설정 | 미응시자 / 점수 미달자 자동 분류 후 재응시 허용 정책 설정 |
| F-02-5 | [B-#06] | 문항 제목 별도 설정 | 문항 본문과 별개로 학생 화면에 노출되는 짧은 제목 입력 |
| F-02-6 | [B-#05] | 정답 판정 옵션 (대소문자/띄어쓰기) | 단답형 정답 비교 시 대소문자/공백 무시 옵션 |
| F-02-7 | [B-#14] | 답변 입력창 확대 | 서술형/단답형 입력 영역 사이즈 확대 |
| F-02-8 | [B-#15] | 답변 입력창 줄바꿈 지원 | textarea 자동 줄바꿈 + 줄 수 동적 확장 |

### 5-3. [C] 자체 도출 개선 - Canvas 기존 기능도 아니고 학교 요구도 아닌, 프로젝트 자체 발굴 개선

| **ID** | **라벨** | **기능** | **동작** |
|---|---|---|---|
| F-02-9 | [C] | 문항 추가 3종 (직접/문제은행/랜덤) 동등 노출 | 문항구성 탭에서 세 방식을 동일 위계 버튼으로 노출 |
| F-02-10 | [C] | 문항 작성 모달 - 우측 예시 문제 동시 노출 | 좌측에서 유형 선택 시 우측에 해당 유형의 예시 문제가 즉시 표시 |
| F-02-11 | [C] | 단계 모달 푸터 통일 (이전 좌측 / 취소+다음 우측) | 모든 스텝 모달이 동일 푸터 레이아웃 |
| F-02-12 | [C] | 추가 기간 설정 분리 (지각 제출 vs 학생별 추가 기간) | 지각 제출(전체 마감일 이후 일괄)과 학생별 추가 기간(특정 대상)을 별도 영역으로 분리 |
| F-02-13 | [C] | 성적 공개 정책 4축 (범위/시점/기간/1회조회) | 공개 범위 + 공개 시점 + 공개 기간 + 1회 조회 옵션을 단일 영역에 통합 |
| F-02-14 | [C] | 안내사항 기본값 자동 채움 | 부정행위/협력 금지 등 표준 안내 문구가 placeholder 가 아닌 실제 기본값으로 채워져 편집 시작 |
| F-02-15 | [C] | 재응시 + 점수 미공개 경고 | 재응시 ON 인데 점수 공개 시점이 모호할 때 저장 직전 확인 다이얼로그 |
| F-02-16 | [C] | 변경 취소 시 작성 데이터 보호 | 폼이 dirty 인 상태에서 취소 클릭 시 ConfirmDialog 로 보호 |
| F-02-17 | [C] | 검증 오류 - 첫 번째 오류만 모달로 명시 | 다수 오류를 나열하지 않고 가장 먼저 위반된 항목 하나만 AlertDialog 로 안내 |
| F-02-18 | [C] | 문제은행에서 직접 추가 | 문제은행 트리에서 개별 문항을 골라 즉시 퀴즈에 편입 |

## 6. 주요 인터랙션

| **#** | **트리거** | **동작** | **결과** |
|---|---|---|---|
| I-1 | 탭 전환 (기본정보 ↔ 문항구성) | 폼 상태 유지하며 탭만 변경 | 입력값 보존 |
| I-2 | 문항 추가 → 직접 작성 | 문항 작성 모달 오픈 | 작성 완료 시 문항 목록에 append |
| I-3 | 문항 추가 → 문제은행 선택 | 은행 선택 모달 오픈 | 선택 문항 일괄 편입 |
| I-4 | 문항 추가 → 랜덤 출제 | 랜덤 출제 모달 오픈 | 그룹별 N문항 랜덤 + 미리보기 |
| I-5 | 문항 드래그 | 드래그 핸들로 순서 변경 | order 즉시 반영 |
| I-6 | 임시저장 | 제목만 검증 후 createQuiz(status=draft) | 목록으로 이동 + "임시저장 완료" 토스트 |
| I-7 | 저장하기 | 전체 검증 → 통과 시 createQuiz | 목록 / 상세로 이동 |
| I-8 | 취소 | dirty 면 ConfirmDialog → 확인 시 목록 이동 | 입력 데이터 폐기 |
| I-9 | 학생별 추가기간 추가 | 학생별 추가 기간 모달 | 학생/그룹 선택 → assignments 배열 추가 |

## 7. 반응형 분기

| **디바이스** | **레이아웃 변화** |
|---|---|
| 모바일 (~767px) | 탭 sticky, 설정 섹션 풀 너비 1열, 푸터 sticky bottom, 모달 전체 화면 |
| 태블릿 (768~1023px) | 설정 섹션 1~2열, 푸터 inline, 모달 90% 너비 |
| 데스크톱 (1024px~) | 설정 섹션 2열 (좌: 기본정보 / 우: 정책), 푸터 inline, 모달 중앙 모달 |

## 8. 비정상 상태 UX

| **상태** | **트리거** | **표현** |
|---|---|---|
| 초기 (LTI) | LTI 진입 + week/session 존재 | 주차/차시 자동 채워진 빈 폼 |
| 초기 (일반) | 사이드바 진입 | 빈 폼, 안내사항만 기본값 |
| 로딩 | LMS 주차/차시 옵션 fetch 중 | 주차/차시 Select 만 Skeleton |
| 작성 중 (dirty) | 사용자 입력 발생 | 취소 시 ConfirmDialog 활성 |
| 검증 실패 | 저장 클릭 시 필수값/날짜 모순/문항 0 등 | AlertDialog 첫 오류 표시 |
| 저장 진행 | 저장 클릭 → API 호출 중 | 버튼 비활성, 스피너 |
| 에러 | createQuiz API 실패 | 토스트 + "다시 시도" 버튼, 폼 데이터 유지 |
| 권한 없음 | 학생이 URL 직접 진입 | 즉시 `/` 로 리다이렉트 (안내 토스트) |
| 오프라인 | 네트워크 단절 | 전역 OfflineBanner, 저장 버튼 비활성 |

## 9. 예외 / 엣지 케이스

- 제목 누락 → "제목을 입력해 주세요" AlertDialog
- 시작일/마감일/이용종료일은 모두 비워둘 수 있음. 단, 값이 있는 경우 시작일 ≤ 마감일 ≤ 이용종료일 순서를 위반하면 날짜 모순 안내
- 지각 제출 ON 인데 마감일 미설정 → 지각 마감 무의미, 검증 차단
- 자동 제출 비활성화 ON 인데 이용종료일 미설정 → 무한정 응시 방지를 위해 검증 차단
- 시간 제한 ON 인데 분 수 0/음수 → 검증 차단
- 문항 0개 + 저장(draft 외) → 검증 차단
- 학생 어사인먼트 중복 (동일 학생 ID 다중 등록) → hasDuplicateStudent 검출 후 검증 차단
- 재응시 1회 → 점수 정책 자동 null 처리 (의미 없음)
- 한 문항씩 표시 ON 시 응답 후 잠금 자동 강제
- LTI 모드: localStorage 의 week/session 값이 LMS 과목에 등록되지 않은 항목이면 무시 후 기본값(1주차/1차시) 적용
- 임시저장 시에는 마감일/문항/접근제어 검증 모두 skip

## 10. 변경 이력

| **날짜** | **버전** | **변경 내용** | **변경자** | **근거** |
|---|---|---|---|---|
| 2026-04-XX | v1 | 초안 작성. 화면 정보 / 영역 구성 / 기능 분류 (A·B·C) / 화면 상태 / 인터랙션 / 예외 6섹션 | 김민주 (Creator/PD) | 초기 SSD 구축 |
| 2026-05-11 | v2 | 외부 공유용 정리 - 구현 위치 컬럼 제거, [C]→[A] 재분류 2건 (A-02-14, A-02-15) | 김민주 (Creator/PD) | Canvas 기능 + UI 라벨/배지 = [A] 원칙 |
| 2026-05-21 | v3 | XP2 [Designer] 가이드 5섹션 + 헤더 + 변경 이력 구조 반영. 사용 컴포넌트(DS Baseline 참조) / 반응형 분기 / 비정상 상태 UX (5종 전체) / 변경 이력 4개 절 신설 | 김민주 (Creator/PD) | XP2 [Designer] Screen Spec, DS Baseline 작성 가이드 (2026-05-26 도입) |
