# XQ-SSD-026. 통계·분석 통합 (Screen Spec)

> **참조 가이드**: XP2 \[Designer\] Screen Spec, DS Baseline 작성 가이드 (페이지 ID 5056888866)
> **본 SSD 범위**: XQ-URD-026 의 통계/분석 영역을 `/quiz/:id/stats` 의 현재 프로토타입 기준으로 명세. 채점 화면 진입은 SSD-025 위임.

## 0. 문서 헤더

| **항목** | **내용** |
|---|---|
| 프로젝트 ID | XQ-202604_01 |
| 문서 ID | XQ-SSD-026-v0.2 |
| 작성자 | 김민주 (Creator) |
| 검토자 | 김범수 (PD) |
| 작성일 | 2026-06-04 |
| 상태 | Draft (PD 검토 전) |
| 참조 URD | [XQ-URD-026](https://xinics.atlassian.net/wiki/spaces/XP2/pages/5081399300) v1.0 |
| 참조 FRD | XQ-FRD-026 |
| 참조 코드 | `src/pages/QuizStats.jsx`, `src/utils/excelUtils.js` (`downloadGradesXlsx`, `downloadItemAnalysisXlsx`) |

---

## 1. 역할별 네비게이션 구조

```
교수자 (instructor):
홈 → 퀴즈 목록 → 카드 메뉴 → "통계" → /quiz/:id/stats
                                       ├── 퀴즈 기본 정보 영역 (채점 화면 이동 진입점)
                                       ├── 학생별 성적 조회 ↔ 퀴즈 전체 통계 (탭/모드 전환)
                                       └── 다운로드 (학생 성적 Excel / 문항 분석 Excel / 파일 업로드 단위)

핵심 태스크 클릭 뎁스:
- 통계 진입: 퀴즈 목록 → 카드 메뉴 → 통계 (3단계)
- Excel 다운로드: 통계 → 다운로드 버튼 (4단계)
- 채점 화면 이동: 통계 → 퀴즈 기본 정보 영역 → 채점 진입 (4단계)
```

**도달 원칙**

- 권한: instructor 단독. 학생 진입 차단.
- 학생별 성적 조회와 퀴즈 전체 통계는 동일 라우트 안에서 전환 (UX-P07-003).
- 측정학 지표(변별도 / 선택지별 응답 패턴 / 상중하위 정답률) 는 적용 가능한 문항 유형에 한정 노출 (UX-P07-060).

---

## 2. 화면 목록

| **화면 ID** | **화면명** | **라우트** | **역할** | **연결 URD 요구사항** | **우선순위** |
|---|---|---|---|---|---|
| SCR-I-STATS | 통계/분석 통합 | `/quiz/:id/stats` | 교수자 | UX-P07-001~003/010~013/020/021/030/040~042/050/060/070~072/080, UX-P05-001/002/003 | P0 |

> 단일 화면. 영역 분기는 §3 내부 서브 섹션으로 명세.

---

## 3. 화면별 상세 설계

### SCR-I-STATS. 통계/분석 통합

**구현 파일**: `src/pages/QuizStats.jsx`

**목적**

퀴즈 종료 후 학생별 성적, 응시 현황, 점수 분포, 문항별 분석, 측정학 지표를 동일 맥락에서 확인하고 Excel 다운로드.

**레이아웃**

```
[PageHeader] 시험명 + 통계 헤더

[퀴즈 기본 정보 영역]
  ├── 시험명 / 응시 기간 / 주차·차시 / 진행 상태
  ├── 설명 (CollapsibleDescription, 길면 line-clamp-2 + 펼치기)
  └── 채점 화면 이동 진입 (Button → /quiz/:id/grade)

[모드/탭 전환]
  └── 학생별 성적 조회 ↔ 퀴즈 전체 통계 (탭)

[학생별 성적 조회 모드]
  ├── 응시 현황 요약 카드
  │    ├── 수강 인원 / 응시 완료 / 미제출 / 채점 완료 / 채점 대기 (인원 + 비율)
  │    └── 분모 정의 안내 (응시자 수 vs 채점 완료 수, 미채점 제외 여부 - XQ-SSD-025 단일화)
  ├── 검색 / 정렬 / 필터 행
  │    ├── 검색 (이름 / 학번)
  │    ├── 정렬 (이름 / 학번 / 학과 / 소요 시간 / 제출일시 / 점수)
  │    └── 필터 (제출 상태 / 채점 상태)
  └── 학생 목록 테이블
       ├── 이름 / 학번 / 학과 / 점수 / 점수 구간 배지 (80↑ / 60~80 / 60↓) / 제출 일시 / 상태
       ├── 점수 미공개 / 수동채점 대기 / 점수 없음 분기 카피
       └── 답안 확인 / 채점 흐름 진입 (행 클릭 → SSD-025)

[퀴즈 전체 통계 모드]
  ├── 응시 요약 (5+2 지표)
  │    ├── 평균 / 최고 / 최저 / 표준편차 / 평균 응시 시간 (Canvas 5 지표)
  │    └── 중앙값 / 참여율 (추가 지표)
  ├── 점수 분포 (히스토그램)
  │    ├── 상위 27% / 중위 46% / 하위 27% 구간별 점수 범위 + 평균 + 누적 추이
  │    └── recharts BarChart (구간별 색상 분기, ReferenceLine 표시)
  ├── 문항별 분석 테이블
  │    ├── 문항 / 유형 / 배점 / 평균 점수 / 득점률 / 난이도 / 채점 현황
  │    ├── 득점률 분류 (쉬움 70%↑ / 보통 / 어려움 40%↓ / 채점 전)
  │    └── 일부 채점 문항은 "채점 완료 N명 / 전체 M명" 동시 표시 (UX-P07-041)
  └── 측정학 지표 테이블 (적용 가능한 문항 유형 한정)
       ├── 변별도 (점이연 상관계수, -1~1)
       ├── 상위 27% 정답률 / 하위 27% 정답률 / 차이
       └── 선택지별 응답 패턴 (객관식 등)

[다운로드 영역]
  ├── 학생 성적 Excel (downloadGradesXlsx)
  ├── 문항 분석 Excel (downloadItemAnalysisXlsx)
  └── 파일 업로드 문항 단위 다운로드 (해당 문항만, 명시 카피)

[빈 상태 분기]
  ├── 응시자 없음 → "아직 응시한 학생이 없습니다" + 응시 안내
  ├── 채점 완료자 없음 → "아직 채점이 완료되지 않았습니다" + 채점 진입 안내
  ├── 검색 결과 없음 → "검색/필터 조건을 조정해 주세요"
  └── 데이터 없음 → 일반 안내
```

**사용 컴포넌트**

| **컴포넌트** | **위치** | **용도** |
|---|---|---|
| `PageHeader` | `src/components/PageHeader.jsx` | 헤더 |
| `Card` / `CardContent` | shadcn | 퀴즈 정보 / 통계 카드 |
| `Badge` | shadcn | 점수 구간 / 득점률 분류 / 측정학 등급 |
| `Button` | shadcn | 다운로드 / 채점 진입 / 정렬 토글 |
| `BarChart` / `Bar` / `XAxis` / `YAxis` / `Tooltip` / `ResponsiveContainer` / `Cell` / `ReferenceLine` | recharts | 점수 분포 히스토그램 |
| `CollapsibleDescription` (내부) | `QuizStats.jsx` | 시험 설명 펼치기 |
| `TypeBadge` (내부) | `QuizStats.jsx` | 문항 유형 배지 |
| Lucide icons | `AlertCircle` `Download` `Search` `ArrowUpDown` `ArrowUp` `ArrowDown` `Check` `ListFilter` | UI 아이콘 |
| `downloadGradesXlsx` / `downloadItemAnalysisXlsx` | `src/utils/excelUtils.js` | Excel 생성 |

**인터랙션 (요약)**

| **#** | **트리거** | **동작** |
|---|---|---|
| I-1 | 진입 (`/quiz/:id/stats`) | quiz / questions / attempts fetch. 로딩 중 적절한 placeholder |
| I-2 | 모드 탭 전환 | 학생별 성적 ↔ 퀴즈 전체 통계 |
| I-3 | 학생 검색 | 이름 / 학번 포함 검색 |
| I-4 | 정렬 변경 | 컬럼 헤더 클릭으로 정렬 토글 (asc / desc / off) |
| I-5 | 필터 변경 | 제출 상태 / 채점 상태 필터 적용 |
| I-6 | 학생 행 클릭 | SSD-025 의 학생 중심 모드 진입 (`/quiz/:id/grade?mode=student&studentId=...`) |
| I-7 | "학생 성적 Excel" 클릭 | `downloadGradesXlsx` 실행 |
| I-8 | "문항 분석 Excel" 클릭 | `downloadItemAnalysisXlsx` 실행 |
| I-9 | 측정학 지표 호버/툴팁 | "변별도는 점수가 높은 학생과 낮은 학생을 얼마나 잘 구분하는지의 지표입니다 (점이연 상관계수, -1~1)" 카피 노출 |
| I-10 | 시험 설명 펼치기 | line-clamp-2 → 전체 표시 토글 |

**상태**

| **상태** | **트리거** | **표현** |
|---|---|---|
| 로딩 | fetch 중 | Skeleton (자체 미구현 시 별도 처리 필요) |
| 응시자 없음 | attempts 0건 | EmptyState - 응시 안내 |
| 채점 완료자 없음 | 채점 완료 0명 | EmptyState - 채점 진입 안내 |
| 점수 구간 80↑ | 점수 ≥ 80 | 상위 점수 구간 배지 |
| 점수 구간 60-80 | 60 ≤ 점수 < 80 | 중간 점수 구간 배지 |
| 점수 구간 60↓ | 점수 < 60 | 하위 점수 구간 배지 |
| 득점률 쉬움 | 득점률 ≥ 70% | "쉬움" 배지 (success) |
| 득점률 보통 | 40% ~ 70% | "보통" 배지 |
| 득점률 어려움 | 득점률 < 40% | "어려움" 배지 (warning) |
| 득점률 채점 전 | 채점 완료자 없음 | "채점 전" 배지 (muted) |
| 변별도 우수 | discrimination ≥ 0.3 | 등급 배지 (success) |
| 변별도 보통 | 0.1 ~ 0.3 | 등급 배지 (warning) |
| 변별도 부족 | discrimination < 0.1 | 등급 배지 (destructive) |
| 일부 채점 문항 | gradedCount < totalCount | "N / M명 채점 완료" 표시 (UX-P07-041) |

---

## 4. 반응형 분기

| **디바이스** | **너비** | **변화** |
|---|---|---|
| 모바일 | ~767px | 학생 목록 카드형, 통계 카드 1열, 히스토그램 가로 스크롤 |
| 태블릿 | 768~1023px | 학생 목록 테이블, 통계 카드 2열, 히스토그램 컨테이너 폭 |
| 데스크톱 | 1024px~ | 학생 목록 테이블 + 통계 카드 3~4열 + 히스토그램 풀폭 |

---

## 5. 비정상 상태 UX

| **상태** | **트리거** | **현재 프로토타입 표현** |
|---|---|---|
| 로딩 | quiz / questions / attempts fetch | (현재 별도 Skeleton 미확인, 진입 직후 동기 렌더 가능성) → 간극 G-1 |
| 응시자 없음 | attempts 0건 | EmptyState |
| 채점 완료자 없음 | 채점 완료 0명 | 통계 영역에 "채점 전" / 빈 상태 카피 |
| 일부 채점 진행 중 | 채점 완료 N명 / 전체 M명 | "N / M명 채점 완료" 표시 (진행 중 값임 인지) |
| 검색 결과 없음 | 검색/필터 결과 0건 | EmptyState 카피 |
| 측정학 지표 적용 불가 | 유형 미적용 (서술형 등) | 해당 행에 "-" 또는 미노출 |
| Excel 생성 실패 | downloadGradesXlsx throw | Toast 또는 console.error (현재 별도 사용자 안내는 미확인) → 간극 G-2 |
| 권한 없음 (학생 직접 URL) | role !== 'instructor' | `<Navigate to="/" replace />` |

---

## 프로토타입과 URD 간극

| **#** | **간극 항목** | **결정** | **처리 상태** |
|---|---|---|---|
| G-1 | 통계 로딩 Skeleton (UX-P07-001 진입 직후 인지) | (B) 백로그 | 현재 별도 Skeleton 미구현 가능성. 로딩 시간이 짧다면 미필요. API 모드 전환 시 검토 |
| G-2 | Excel 생성 실패 사용자 안내 | (B) URD 완화 | **URD-026 v0.3 정정 완료** (2026-06-05) — UX-P07-073 신규 추가 (Excel 생성 실패 시 표준 Toast 안내) |
| G-3 | 학생 식별 정보 / 교수자 메모 접근 권한 (UX-P05-003, OQ-XQ-URD-026-03) | (B) 법무 검토 의존 | URD OQ 의존. 법무 검토 후 권한 분기 추가 |
| G-4 | TA(P-03) / 운영자(P-05) 분기 | (B) URD 완화 | **URD-026 v1.0 정정 완료** (2026-06-05) — Canvas 권한 비트 위임으로 정리. P-11 페르소나는 강사의 통계 측면임을 명시 |
| G-5 | 측정학 지표 적용 가능 문항 유형 안내 (UX-P07-060 해석 방향) | (A) 부분 충족 | 변별도 / 선택지별 응답 패턴 등 적용 가능 유형은 현재 자동 계산. 적용 불가 유형의 명시 카피는 C 분류 후속 카피 작업 |


---

## 변경 이력

| **날짜** | **버전** | **변경 내용** | **변경자** |
|---|---|---|---|
| 2026-06-05 | v0.2 | 간극 표 G-2/G-4 URD-026 정정 완료 반영 (v0.3/v1.0). 참조 URD 버전 v1.0 으로 갱신 | 김민주 (Creator) |
