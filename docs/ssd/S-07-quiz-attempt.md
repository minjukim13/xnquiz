# S-07. 학생 응시

> **라벨 규칙**: `[A]` Canvas 기존 기능 / `[B-#NN]` 학교 요구사항 신규 / `[C]` 자체 도출 개선

## 0. 문서 헤더

| **항목** | **내용** |
|---|---|
| 프로젝트 ID | PRJ-XQ-BASE |
| 문서 ID | PRJ-XQ-BASE-SSD-S-07-v3 |
| 작성자 | 김민주 (Creator/PD) |
| 참조 FRD | FRD-001 등 (TBD) |
| 참조 DS Baseline | LearningX DS Baseline (예정). 임시: SSD [README](README.md) 디바이스 분기점 / 컴포넌트 사용 원칙 절 |

## 1. 화면 정보

| **항목** | **내용** |
|---|---|
| 화면 ID | S-07 |
| 라우트 | `/quiz/:id/attempt` (옵션: `?preview=true`, `?retake=true`) |
| 진입 권한 | 학생 (실제 응시) / 교수자 (미리보기 only) |
| 진입 경로 | 퀴즈 상세(S-03) → 응시하기/재응시 / 퀴즈 카드 메뉴 → 미리보기 |
| 우선순위 | P0 |

## 2. 화면 목적

- 학생이 실제 퀴즈를 풀고 제출 (시간 제한 / 자동 저장 / 활동 로그 추적)
- 교수자가 학생 시점 그대로 미리보기 (실제 기록 없음)

## 3. 레이아웃 (Region Map)

```
[헤더: 퀴즈 제목 | 주차·차시·만점 | 설명 | 자동 저장 상태]
[우측 상단: 남은 시간 카운트다운 (MM:SS) | 답변 완료 진행률]
[본문: 문항 카드 (전체 표시 또는 한 문항씩)]
[네비게이션: 이전 / 다음 또는 제출]
[배너 (조건부): 자동 저장 실패 / 지각 제출 / 미리보기 모드]
[모달: 제한시간 만료 / 응답 후 잠금 경고 / 미답변 스킵 경고 / 제출 결과]
```

| **영역** | **설명** |
|---|---|
| 헤더 | 메타 + 자동 저장 상태 |
| 우측 상단 | 타이머 + 진행률 |
| 본문 | 문항 카드 (전체/단일) |
| 네비게이션 | 이전/다음/제출 |
| 배너 | 조건별 안내 (자동 저장 실패/지각/미리보기) |
| 모달 | 만료/잠금/스킵/결과 |

## 4. 사용 컴포넌트 (DS Baseline 참조)

| **컴포넌트** | **variant / state** | **용도** |
|---|---|---|
| Timer | countdown / overtime-grace | MM:SS 카운트다운 + 5분 grace |
| QuestionCard | mcq / short / long / formula / file-upload 등 12종 | 문항 유형별 렌더 |
| Input / Textarea | enlarged (B-#14 / B-#15) | 단답/서술 입력 |
| Button | primary (제출) / outline (이전·다음) / ghost (종료) | 네비게이션 |
| ProgressBar | answered | 답변 완료 진행률 |
| Banner | info (자동 저장) / warning (지각) / preview (미리보기) | 조건부 배너 |
| AlertDialog | time-up / lock-confirm-normal / lock-confirm-warn / unanswered-submit | 4종 경고 |
| ResultModal | submitted / auto-submitted | 제출 완료 |
| Skeleton | attempt | 로딩 placeholder |

## 5. 기능 분류

### 5-1. [A] Canvas 기존 기능 - 기존 Canvas Classic Quizzes 제공 기능 재구현

| **ID** | **기능** | **동작** |
|---|---|---|
| A-07-1 | 12개 문항 유형 렌더링 | 객관식/단답/서술/공식/파일업로드 등 |
| A-07-2 | 시간 제한 카운트다운 | 1초 간격 감소, 0 도달 시 자동 제출 |
| A-07-3 | 자동 저장 (30초 간격 + 변경 시) | localStorage 에 answers/currentIndex/startedAt 저장 + 서버 동기화 |
| A-07-4 | 한 문항씩 표시 (oneQuestionAtATime) | 단일 문항 + 진행률 + 이전/다음 버튼 |
| A-07-5 | 응답 후 잠금 (lockOnAnswer) | 한 문항씩 모드에서 다음 이동 후 이전 비활성 |
| A-07-6 | 자동 채점 | 제출 시 autoGrade 항목 즉시 점수화 |
| A-07-7 | 제출 → 결과 화면 | submitAttempt → 점수 표시 → 결과 페이지 리다이렉트 |
| A-07-8 | 새로고침/재접속 시 세션 복원 | localStorage 의 sessionKey 로 currentIndex/answers 복원 |
| A-07-9 | 액세스 코드 / IP 검증 | 응시 진입 시 정책 검증 (서버 영역 일부) |
| A-07-10 | 미리보기 모드 (preview=true) | 시간 제한 무시 + 세션 저장 안 함 + 활동 로그 안 함 + 제출 시 DB 미기록 |
| A-07-11 | 자동 제출 결과 명시 | ResultModal 에 "시간 종료 - 자동 제출되었습니다" |

### 5-2. [B] 학교 요구사항 반영 - Canvas 에 없거나 부족하여 학교가 요구한 신규 기능

| **ID** | **라벨** | **기능** | **동작** |
|---|---|---|---|
| F-07-1 | [B-#14] | 답변 입력창 확대 | 서술형/단답형 입력 영역 사이즈를 Canvas 기본보다 크게 |
| F-07-2 | [B-#15] | 답변 입력창 줄바꿈 | textarea 자동 줄바꿈 + 줄 수 동적 확장 |

### 5-3. [C] 자체 도출 개선 - Canvas 기존 기능도 아니고 학교 요구도 아닌, 프로젝트 자체 발굴 개선

| **ID** | **라벨** | **기능** | **동작** |
|---|---|---|---|
| F-07-3 | [C] | 미리보기 배너 | 황색 배너 + "미리보기 모드" 라벨 + 종료 버튼 |
| F-07-4 | [C] | 답변 변경 활동 로그 집계 | 빠른 입력 시에도 일정 간격으로 답변 변경 이벤트를 모아서 기록 (활동 로그 과다 방지) |
| F-07-5 | [C] | 활동 로그 정밀 추적 | 시작 / 답변 변경 / 자동 저장 / 문항 이동 / 포커스 이탈 / 포커스 복귀 / 제출 각 이벤트를 시각화 |
| F-07-6 | [C] | 포커스 이탈 감지 | 탭/창 전환 시점을 감지하여 활동 로그에 주의 색상으로 강조 |
| F-07-7 | [C] | 자동 저장 상태 라벨 | 헤더에 "마지막 저장: HH:MM:SS" 표시 |
| F-07-8 | [C] | 진행률 시각화 | 답변 완료 N/전체 + progress bar |
| F-07-9 | [C] | 한 문항씩 모드 - 첫 진입 안내 | "응답 후 문항 잠금" 사전 안내 모달 |
| F-07-10 | [C] | 응답 후 잠금 - 두 단계 경고 | 답변 완료 후 다음 = 일반 confirm / 미답변 후 다음 = 빨강 강조 confirm |
| F-07-11 | [C] | 응시 진입 차단 (5종) | 이용종료일 경과 / 시작일 미도래 / 초안 / 채점중 / 지각 마감 경과 등 5종 사전 차단 + 안내 화면 |

## 6. 주요 인터랙션

| **#** | **트리거** | **동작** | **결과** |
|---|---|---|---|
| I-1 | 답변 입력 | 디바운스 1.5초 후 ANSWER_CHANGE 로그 + dirtyRef true | 자동 저장 큐 |
| I-2 | 다음/이전 버튼 | NAVIGATE 로그 + currentIndex 변경 | 새 문항 표시 |
| I-3 | 다음 버튼 (잠금 모드 + 답변 완료) | ConfirmDialog 일반 톤 | 확인 시 이동 |
| I-4 | 다음 버튼 (잠금 모드 + 미답변) | ConfirmDialog 빨강 강조 | 확인 시 이동 (점수 손실 가능) |
| I-5 | 자동 저장 (30초) | localStorage + 서버 동기화 | 저장 상태 라벨 갱신 |
| I-6 | 포커스 이탈 | FOCUS_LOSS 로그 | 채점 대시보드 활동 로그에 amber 강조 표시 |
| I-7 | 시간 만료 | handleSubmit(auto=true) | ResultModal "자동 제출" 표기 |
| I-8 | 제출 클릭 | 미답변 0개면 즉시 / 1개 이상이면 ConfirmDialog | 제출 진행 |
| I-9 | 제출 완료 | ResultModal | "결과 자세히 보기" → /quiz/:id |
| I-10 | 미리보기 종료 | 학생 결과 화면 진입 (실제 기록 없음) | 퀴즈 상세(S-03) |

## 7. 반응형 분기

| **디바이스** | **레이아웃 변화** |
|---|---|
| 모바일 (~767px) | 타이머 sticky top, 문항 카드 풀 너비, 네비게이션 sticky bottom, 자동 저장 상태는 헤더 우측 아이콘 only |
| 태블릿 (768~1023px) | 타이머 + 진행률 헤더 우측 inline, 문항 카드 중앙 정렬 |
| 데스크톱 (1024px~) | 타이머 + 진행률 우측 상단 고정, 문항 카드 중앙 정렬 (최대 너비 제한) |

## 8. 비정상 상태 UX

| **상태** | **트리거** | **표현** |
|---|---|---|
| 로딩 | 초기 fetch / 세션 복원 | Skeleton |
| 진입 차단 | lockDate / startDate / draft / grading / 지각 마감 경과 | 차단 화면 + "응시 시작 전" / "마감 후" / "초안 상태" 등 5종 안내 |
| 미리보기 | preview=true | 황색 배너 + 종료 버튼 + 시간 무시 |
| 응시 중 | 정상 진입 + 시간 남음 | 카운트다운 + 자동 저장 진행 |
| 응답 후 잠금 모드 | oneQuestionAtATime + lockAfterAnswer | 이전 비활성 + 다음 이동 시 확인 |
| 시간 만료 | timeRemaining ≤ 0 | 자동 제출 비활성화 OFF: 즉시 자동 제출 / ON: 헤더 "시간 종료 - 5:00 grace" 카운트다운, 0 도달 시 강제 자동 제출 |
| 제출 진행 | handleSubmit | 버튼 비활성 + 스피너 |
| 제출 완료 | API 응답 성공 | ResultModal 표시 |
| 자동 저장 실패 | 네트워크 일시 단절 | 헤더 배너 "자동 저장 실패 - 재시도" + 백오프 재시도 |
| 에러 (제출 실패) | submitAttempt 실패 | AlertDialog "다시 시도" 버튼 + 본인 답안 보존 |
| 권한 없음 | 학생이 아닌데 preview=false 로 진입 | 즉시 `/` 로 리다이렉트 |
| 오프라인 | 네트워크 단절 | 전역 OfflineBanner, 자동 저장은 큐잉하여 복귀 시 일괄 전송, 제출은 차단 |

## 9. 예외 / 엣지 케이스

- 새로고침 후 시간 차감 누적 반영 - 재접속 시 startedAt 으로 경과 시간 계산
- 시간 만료(timeRemaining=0) - 기본 자동 제출. "자동 제출 비활성화" 토글 ON 인 경우 5분 grace 동안 학생 수동 제출 대기, 5분 경과 시 강제 자동 제출
- 시간 초과 상태에서 재접속 - 자동 제출 비활성화 OFF: 즉시 자동 제출 / ON: 잔여 grace 시간만큼 추가 대기 후 자동 제출
- 자동 제출 비활성화 ON 인 경우 lockDate(이용종료일) 입력 필수 (저장 시 검증 차단) - 무한정 응시 방지
- 미리보기 모드 - 세션 저장/활동 로그/시간 제한/DB 기록 모두 차단
- API 제출 실패 - AlertDialog "다시 시도" 버튼 + 본인 답안 보존
- 한 문항씩 모드 + 응답 후 잠금 - 점수 손실 위험 명시
- 부정행위 가능성 - 포커스 이탈 다회 감지 시 채점 대시보드에서 amber 강조
- lockDate(이용종료일) 도달 시 응시 중 - 답안 보호를 위해 무조건 자동 제출 (자동 제출 비활성화 토글과 무관)

## 10. 변경 이력

| **날짜** | **버전** | **변경 내용** | **변경자** | **근거** |
|---|---|---|---|---|
| 2026-04-XX | v1 | 초안 작성. 응시 + 자동 저장 + 활동 로그 + 미리보기 + 시간 만료 정책 정의 | 김민주 (Creator/PD) | 초기 SSD 구축 |
| 2026-05-11 | v2 | 외부 공유용 정리 - 구현 위치 컬럼 제거, [C]→[A] 재분류 2건 (A-07-10 미리보기, A-07-11 자동 제출 결과), 자동 제출 비활성화 정책 정정 | 김민주 (Creator/PD) | Canvas 기능 + UI 라벨/배지 = [A] 원칙, 자동 제출 비활성화 토글 정책 명문화 |
| 2026-05-21 | v3 | XP2 [Designer] 가이드 5섹션 + 헤더 + 변경 이력 구조 반영. 사용 컴포넌트(DS Baseline 참조) / 반응형 분기 / 비정상 상태 UX (5종 전체) / 변경 이력 4개 절 신설 | 김민주 (Creator/PD) | XP2 [Designer] Screen Spec, DS Baseline 작성 가이드 (2026-05-26 도입) |
