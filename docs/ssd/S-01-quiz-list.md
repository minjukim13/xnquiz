# S-01. 퀴즈 목록

> **라벨 규칙**: `[A]` Canvas 기존 기능 / `[B-#NN]` 학교 요구사항 신규 / `[C]` 자체 도출 개선

## 1. 화면 정보

| **항목** | **내용** |
|---|---|
| 화면 ID | S-01 |
| 라우트 | `/` |
| 진입 권한 | 교수자 / 학생 - 로그인 시점에 부여된 역할에 따라 분기 |
| 진입 경로 | 사이드바 "퀴즈" / LearningX LTI 진입 시 기본 랜딩 |
| 구현 파일 | QuizList.jsx |
| 연계 FRD | A-01, A-12, R-B-12, R-B-03 |

## 2. 화면 목적

- **교수자**: 담당 과목의 모든 퀴즈를 한 화면에서 조회 / 생성 / 편집 / 채점 / 통계 진입
- **학생**: 본인이 응시 가능한 퀴즈 + 응시 이력을 카드 단위로 조회

## 3. 영역 구성 (Region Map)

| **영역** | **설명** | **주요 컴포넌트** |
|---|---|---|
| 헤더 | 페이지 타이틀 ("퀴즈 관리" / "내 퀴즈") + 전역 설정 + 가져오기/새 퀴즈 버튼 | Button, Settings2 icon |
| 필터/정렬 바 | 주차/차시 필터 + 정렬 드롭다운 | WeekSessionFilter, DropdownSelect |
| 본문 | 퀴즈 카드 그리드 (교수자: QuizCard / 학생: StudentQuizCard) | Card, StatusBadge, VisibilityBadge |
| 빈 상태 | 조건 충족 퀴즈 0건일 때 안내 | 아이콘 + 안내 문구 |
| 로딩 | API 모드 fetch 중 | QuizCardSkeleton |
| 모달 | 퀴즈 복사 / 가져오기 / 전역 설정 / 삭제 확인 | QuizCopyModal, QuizImportModal, QuizSettingsDialog, ConfirmDialog |

## 4. 기능 분류

### 4-1. [A] Canvas 기존 기능 - 기존 Canvas 제공 기능 재구현

| **ID** | **기능** | **동작** | **구현 위치** |
|---|---|---|---|
| A-01-1 | 퀴즈 목록 조회 | 과목에 속한 모든 퀴즈를 카드 형태로 조회 | InstructorQuizList |
| A-01-2 | 퀴즈 생성 진입 | `새 퀴즈` 버튼 → `/quiz/new` 이동 | 헤더 영역 |
| A-01-3 | 퀴즈 편집 진입 | 카드 메뉴 → `편집` → `/quiz/:id/edit` | DropdownMenuItem |
| A-01-4 | 퀴즈 삭제 | 카드 메뉴 → `삭제` → 확인 다이얼로그 → 삭제 처리 | ConfirmDialog + deleteQuiz |
| A-01-5 | 퀴즈 복사 | 카드 메뉴 → `복사` → 대상 과목 선택 → 신규 퀴즈 생성 + 문항 복제 | QuizCopyModal |
| A-01-6 | 학생/교수자 역할별 화면 분기 | 로그인 역할에 따라 다른 목록 렌더 | QuizList 라우트 컴포넌트 |
| A-01-7 | 학생 - 응시 가능 퀴즈 조회 | `draft` 제외 + `visible !== false` 필터 | StudentQuizList |
| A-01-8 | 학생 - 응시 이력 표시 | 카드에 본인 attempt 의 점수/제출일 표시 | StudentQuizCard |

### 4-2. [B] 학교 요구사항 반영 - Canvas 에 없거나 부족하여 학교가 요구한 신규 기능

| **ID** | **라벨** | **기능** | **동작** | **구현 위치** |
|---|---|---|---|---|
| F-01-1 | [B-#12] | 시험 전용 목록 + 주차/차시 동시 노출 | 카드에 `n주차 m차시` 배지 표시. Canvas 시험 메뉴는 주차 정보 없음 | QuizCard |
| F-01-2 | [B-#12] | 주차/차시 필터링 | 주차 선택 시 차시 옵션이 동적으로 좁혀짐 | WeekSessionFilter |
| F-01-3 | [B-#03] | 다른 과목에서 퀴즈 가져오기 | `가져오기` 버튼 → 과목/퀴즈 다중 선택 → 일괄 복사. LTI 모드는 Canvas REST 로 교수자 담당 과목 전체 조회 | QuizImportModal |
| F-01-4 | [B-#03] | 복사/가져오기 후 자동 초기화 안내 | 복사 시 주차/응시기간/공개정책/지각제출/접근제어를 자동 초기화하고, 안내 박스로 사전 고지 | ResetNotice, resetFields |

### 4-3. [C] 자체 도출 개선 - Canvas 기존 기능도 아니고 학교 요구도 아닌, 프로젝트 자체 발굴 개선

| **ID** | **라벨** | **기능** | **동작** | **구현 위치** |
|---|---|---|---|---|
| F-01-5 | [C] | 정렬 옵션 4종 | 최근생성 / 주차 오름차순 / 주차 내림차순 / 마감임박순 | SORT_OPTIONS, sortQuizzes |
| F-01-6 | [C] | 3단계 상태 배지 (초안/진행중/마감) | DB status (`draft/open/closed`) + `dueDate` 자동 판정 → 표시 상태로 변환 | resolveDisplayStatus, StatusBadge |
| F-01-7 | [C] | D-day 배지 | 진행중 퀴즈의 마감일까지 남은 일 수를 `D-N` 형식으로 표시. 당일은 빨강, 그 외는 주황 | getDdayBadge |
| F-01-8 | [C] | 학생 공개/비공개 토글 | 카드 메뉴에서 1클릭 토글, 임시저장은 자동 비공개 (강제) | handleToggleVisibility, VisibilityBadge |
| F-01-9 | [C] | 인라인 통계 (응시율/응시인원/미제출/평균점수) | 카드 우측에 4개 핵심 지표 노출, 미제출 0 초과 시 빨강 강조 | getInlineStats, InlineStat |
| F-01-10 | [C] | 지각 제출 안내 라벨 | 지각 제출 허용 시 "지각 제출: yyyy-mm-dd 까지" 또는 "무제한 허용" 표기 | QuizCard 본문 |
| F-01-11 | [C] | 카드 전체 클릭 → 상세 진입 / 메뉴는 stopPropagation | 카드 어디든 클릭으로 상세, 메뉴 영역은 별도 인터랙션 | QuizCard onClick |
| F-01-12 | [C] | 미리보기 진입 | 카드 메뉴 → `미리보기` → `/quiz/:id/attempt?preview=true` (교수자 전용 학생 뷰) | DropdownMenuItem |
| F-01-13 | [C] | 채점/통계 직접 진입 | 카드 메뉴 → `채점` / `통계` (응시 가능 상태일 때만 노출) | canGrade 분기 |
| F-01-14 | [C] | 퀴즈 전역 설정 진입점 | 헤더 톱니바퀴 → 전역 설정 다이얼로그 (전 퀴즈 공통 설정) | QuizSettingsDialog |
| F-01-15 | [C] | LTI 자동 진입 처리 | LearningX 진입 시 토큰/role/과목코드/주차/차시 파싱 → localStorage 저장 → 과목 자동 필터 | App.jsx LTI 부트스트랩 |
| F-01-16 | [C] | 임시저장 - 학생 비공개 자동 강제 | `draft` 상태는 visible 토글과 무관하게 항상 비공개 표시 | isDraft 분기 |
| F-01-17 | [C] | 이용 종료(lockDate) 학생 카드 처리 | 학생 화면에서 lockDate 경과 시 카드 자체를 lock 표시로 대체 | isLockDatePassed |

## 5. 화면 상태 (State)

| **상태** | **트리거** | **표현** |
|---|---|---|
| 로딩 | API 모드 + 초기 fetch 중 | QuizCardSkeleton × 3 |
| 빈 상태 (필터) | 데이터 0건 + 필터 적용 중 | FileText 아이콘 + "해당 조건에 맞는 퀴즈가 없습니다" |
| 빈 상태 (학생, 미수강) | 학생 뷰에서 응시 가능 퀴즈 0건 | "현재 응시 가능한 퀴즈가 없습니다" |
| 정상 | 데이터 1건 이상 | 카드 그리드 |
| Toast 알림 | 복사/가져오기/공개여부/삭제 등 결과 안내 | Toast 컴포넌트 |
| 모달 (복사/가져오기/삭제/전역설정) | 메뉴/버튼 트리거 | 각 모달 컴포넌트 |

## 6. 주요 인터랙션

| **#** | **트리거** | **동작** | **결과** |
|---|---|---|---|
| I-1 | 카드 본문 클릭 | `/quiz/:id` 이동 | 퀴즈 상세 (S-03) |
| I-2 | `새 퀴즈` 버튼 | `/quiz/new` 이동 | 퀴즈 생성 (S-02) |
| I-3 | `가져오기` 버튼 | 가져오기 모달 오픈 | 다른 과목에서 퀴즈 가져오기 |
| I-4 | 톱니바퀴 아이콘 | 전역 설정 다이얼로그 오픈 | 전역 설정 변경 |
| I-5 | 카드 메뉴 → 편집 | `/quiz/:id/edit` | 퀴즈 편집 (S-04) |
| I-6 | 카드 메뉴 → 미리보기 | `/quiz/:id/attempt?preview=true` | 학생 응시 화면 (S-07, preview) |
| I-7 | 카드 메뉴 → 복사 | 복사 모달 오픈 | 대상 과목 선택 후 복사 |
| I-8 | 카드 메뉴 → 학생에게 공개/숨기기 | 공개여부 업데이트 | 토스트 + 배지 갱신 |
| I-9 | 카드 메뉴 → 채점 | `/quiz/:id/grade` | 채점 대시보드 (S-05) |
| I-10 | 카드 메뉴 → 통계 | `/quiz/:id/stats` | 통계 (S-06) |
| I-11 | 카드 메뉴 → 삭제 | 삭제 확인 다이얼로그 → 삭제 | 토스트 + 목록 갱신 |
| I-12 | 주차 필터 변경 | 차시 옵션 자동 재계산 + 필터 적용 | 카드 목록 갱신 |
| I-13 | 정렬 변경 | 정렬 함수 재실행 | 카드 순서 갱신 |

## 7. 예외/엣지 케이스

- 임시저장(`draft`) 퀴즈는 학생에게 노출 금지 (StudentQuizList 에서 필터)
- 임시저장 퀴즈는 카드 메뉴에서 공개/숨기기 비활성 + "임시저장 상태에선 자동 비공개입니다" 툴팁
- 예약 발행(`startDate` 미래)은 `scheduled` 상태로 별도 표시, D-day/통계 비노출
- 마감일 경과 시 DB status 는 그대로 `open` 이지만 화면 상태만 `closed` 로 표시 (resolveDisplayStatus)
- 지각 제출 허용 + lateSubmitDeadline 미설정 시 "무제한 허용" 표기
- 학생 화면에서 lockDate 경과 시 카드 내용 자체를 차단 (isLockDatePassed)
- LTI 모드에선 서버가 과목 필터링 수행 → 클라이언트 `course === CURRENT_COURSE` 체크 skip
- 가져오기 모달에서 LTI 모드는 Canvas REST 로 교수자 담당 과목 전체 조회 (mock 모드는 MOCK_COURSES 사용)

## 8. 미구현 / Open Issue

- 퀴즈 검색(텍스트 키워드) 기능 없음 - 현재는 주차/차시 필터 + 정렬만 제공
- 페이지네이션/무한 스크롤 미적용 - 퀴즈 30개 이상 시 성능 검증 필요
- 학생 화면 정렬 옵션은 교수자와 동일하지만, 본인 응시 이력 기준 정렬(미응시/완료 우선) 별도 필요할 수 있음
